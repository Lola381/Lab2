PORT=8000

# Paste the connection string from Atlas: Database > Connect > Drivers.
# Replace the username, password, cluster host, and database name.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/bookstore?retryWrites=true&w=majority

JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1h